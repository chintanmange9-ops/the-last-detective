"""Developer tools (solver bot, etc.). None of this is part of the game
itself - it exists to prove every generated case is actually solvable,
using exactly the same command surface a human player sees."""